
# Следующий интересный файл – /etc/hosts.deny
По умолчанию в этом файле запрещаются все соединения. Поправить это можно закомментировав строку ALL: ALL: DENY, т.е. поставить # перед фразой решетку, вот так:
# ALL: ALL: DENY

# В /etc/resolv.conf настраиваются три основные опции (директивы):
nameserver # IP адрес DNS сервера
domain # Доменное Имя локального хоста
search # Какие Доменные Имена искать

# Опция nameserver указывает на IP адрес DNS сервера.

# Это может быть Ваш личный DNS сервер, какой-либо публичный DNS сервер либо DNS сервер Вашего интернет провайдера.

# Замечание: Вы можете указать максимум 3 DNS сервера.

# Пример:

nameserver 192.168.0.100
nameserver 8.8.8.8
nameserver 8.8.4.4


# Локальное доменное имя хоста.

domain <локальное доменное имя>

# Пример:

domain domain.com
nameserver 192.168.0.100
nameserver 8.8.4.4




# Установим SSH сервер:

pacman -S openssh

# Добавим его в автозагрузку:

systemctl enable sshd.service

#Теперь мы можем подключиться к нашему серверу по SSH:
# ssh имя_пользователя@ip_сервера

#Например:

ssh archuser@10.0.2.15

# Port: хотя порт 22 используется по умолчанию, его можно заменить любым номером в диапазоне от 1025 до 65536. В данном примере используется порт 25000. Не забудьте записать новый номер порта. В дальнейшем он понадобится при входе на сервер. Данное изменение усложнит вход для неавторизованных пользователей.
# PermitRootLogin: чтобы деактивировать вход с root, измените в данной строке yes на no. С этого момента на сервер можно войти только через нового пользователя.


nano /etc/ssh/sshd_config
Port 25000
PermitRootLogin no

# Перезагрузите SSH, чтобы привести новые порты и настройки в исполнение:
systemctl restart sshd
# Чтобы проверить новые настройки (не выходите из root), откройте новое окно терминала и войдите как новый пользователь.
# Не забудьте внести новый номер порта.
ssh -p 25000 exampleuser@123.45.67.890
# Появится извещение:
# [exampleuser@yourhostname ~]$





# почтовый сервер postfix
sudo pacman -S postfix



# LAMP — Linux + Apache + MySQL + PHP
sudo pacman -S apache php php-apache mysql

# php-fpm mod_fcgid php-cgi

sudo pacman -S php-mcrypt phpmyadmin




# Сервер
Установка и настройка ssh
	установка ssh # info ip adress show
	Настройка ssh
		Изменить порт (22) по умолчанию # в диапазоне от 1025 до 65536.
		Деактивировать вход с root
Почтовый сервер postfix
LAMP # checklist linux + Apache + MySQL + PHP
	apache php php-apache mysql php-mcrypt phpmyadmin
Настройка ip адреса вручную
Назад








